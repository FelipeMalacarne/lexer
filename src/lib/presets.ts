export const PRESETS: Record<string, string[]> = {
  default: ['begin', 'end', 'if', 'else', 'while', 'return'],
  programming: ['function', 'var', 'let', 'const', 'class', 'import', 'export'],
  custom: [], // Permite adicionar palavras personalizadas
};
